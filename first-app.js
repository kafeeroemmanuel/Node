const fs = require("fs"); //fs is the file system for interacting with files.

fs.writeFileSync("hello.txt", "Hello from Nodejs"); // write to add a file on my hard drive. 2 args, pathname and content
